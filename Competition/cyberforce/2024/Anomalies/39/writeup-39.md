When inspecting the API documentation you will find 

{
  "name": "string",
  "description": "string",
  "admin_privileges": false
}


in /role/create - which is obviously bad because someone can take that API and change their role from false to truewq

